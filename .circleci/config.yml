version: 2
jobs:
  lint:
    docker:
      - image: koalaman/shellcheck-alpine
    steps:
      - checkout
      - run:
          name: lint
          command: |
            shellcheck -x build.sh
            shellcheck -x tag.sh
  build:
    docker:
      - image: circleci/golang:1.11.1
    working_directory: /go/src/github.com/helm/chart-testing
    steps:
      - setup_remote_docker
      - run:
          name: Install tools
          command: |
            curl -SLO https://github.com/goreleaser/goreleaser/releases/download/v0.95.2/goreleaser_Linux_x86_64.tar.gz
            sudo mkdir -p /usr/local/goreleaser
            sudo tar -xzf goreleaser_Linux_x86_64.tar.gz -C /usr/local/goreleaser
            sudo ln -s /usr/local/goreleaser/goreleaser /usr/local/bin/goreleaser
            rm -rf goreleaser_Linux_x86_64.tar.gz
            curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
      - checkout
      - run:
          name: Unit test
          command: |
            dep ensure -v
            go test -race -coverprofile=coverage.txt -covermode=atomic -tags teste2e ./...
            bash <(curl -s https://codecov.io/bash)
      - run:
          name: Build
          command: |
            set -e
            set -u

            if [[ -z "${CIRCLE_TAG:-}" ]]; then
              echo "Building snapshot..."
              ./build.sh
            else
              echo "Building release $CIRCLE_TAG..."
              echo $DOCKER_PASSWORD | docker login --username $DOCKER_USERNAME --password-stdin quay.io
              ./build.sh --release
            fi
workflows:
  version: 2
  untagged-build:
    jobs:
      - lint
      - build
  tagged-build:
    jobs:
      - build:
          filters:
            tags:
              only: /^v.*/
            branches:
              ignore: /.*/
